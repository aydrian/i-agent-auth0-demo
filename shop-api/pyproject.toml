[project]
name = "shop-api"
version = "0.1.0"
description = "FastAPI server for online shopping demo"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "jinja2",
    "pydantic",
    "python-multipart",
    "rapidfuzz",
    "uvicorn[standard]",
]

[dependency-groups]
dev = []
